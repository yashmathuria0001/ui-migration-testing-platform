{
  "name": "ui-migration-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright agent for UI Migration Testing Platform",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "install:browsers": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}

